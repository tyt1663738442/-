# 2026-05-18 工作记录

## 修复东方财富API参数bug（hot_trend.py）
- 原第264行 params 字典包含错误的URL字符串 `'sr=-1&page_size=50...'`
- 拆分为独立的URL基础和params字典：`url` + `params = {'sr': '-1', 'page_size': 50, ...}`
- 修复后东方财富公告API参数正确传递

## 当前状态（00:31）
- 后端运行在 localhost:8001 ✅
- 前端运行在 localhost:8000 ✅
- 最强风口API正常返回50只股票数据 ✅
- 所有历史任务均已完成（卡片筛选/端口统一/数据修复/Railway适配/GitHub推送/新闻时间优化）

## 最强风口v2分层评分优化（09:00）
- 重写 hot_trend.py：分层情绪关键词库（政策/行业/资金/业绩/国际/通用）
- 新增 classify_news_type()：混合模式分类（API字段优先 + 关键词兜底）
- 新增 detect_force_level()：政策力度分级（国家级×2.5 / 部位级×2.0 / 地方级×1.0 / 行业级×0.5）
- 新增 analyze_sentiment_by_type()：按消息类型分层情绪分析
- 新增 extract_stock_code_from_text()：从公告中提取股票代码
- 新增 generate_impact_reason()：生成影响说明文本
- 评分公式：score_change = base_score × 情绪系数 × 力度系数
- base_score：政策20分、行业/资金/业绩/国际15分、通用10分
- 个股匹配策略：精准提取代码 + 自上而下板块传导
- 前端 HotTrendPage.tsx v2：类型过滤标签、分数构成可视化、impact_reason展示
- GitHub推送成功：`feat: 最强风口分层评分逻辑优化 v2`

## 竞价选股演示数据模式（09:19）
- 问题：非交易时段/本地开发时，竞价选股公式返回0只股票，页面空白
- 原因：新浪API在非交易时间 `amount` 为0，`float_cap`/`turnover` 获取失败，公式条件严格匹配不上
- 修复：
  1. `main.py` 新增 `_get_demo_formula_data()`：为5大公式各生成7-8只模拟股票，数据合理
  2. `auction_formula_scan` 路由：当 `results` 为空或异常时，自动返回演示数据并标记 `is_demo: true`
  3. `_parse_sina_line` 修复 `volume_ratio` bug（原用 `parts[39]` PE字段错误赋值）
  4. `FormulaPanel.tsx`：新增 `isDemo` state，统计栏显示"演示数据"黄色标签
- GitHub推送：`feat: 竞价选股增加演示数据模式，优化本地无数据问题`

## 复盘分析连板数修复 + 公告匹配优化（15:07）
- **连板数总为1的bug**：`_update_board_count_cache` 在非交易日（周末）返回空DataFrame（0行）但不抛异常，代码直接 `return` 导致取不到交易日数据
  - 修复：跳过空结果，继续往前找最近的交易日；扩大搜索范围到10天
  - 数据源正常：akshare `stock_zt_pool_em(date='20260515')` 返回54只涨停，其中6只连板>1
  - 验证结果：利仁科技5连板、京能电力3连板、北自科技2连板
- **个股公告匹配**：移除 `sector_index` 板块传导匹配（`get_hot_trend_data`），只保留：
  1. 精准匹配：从公告中提取股票代码直接命中
  2. 名称匹配：新闻标题关键词命中个股名称
- GitHub推送：`fix: 修复连板数总为1的bug + 移除个股公告的板块传导匹配`

## 个股公告关键词串股修复（15:35）
- **问题**：威帝股份的公告出现在科陆电子(002121)页面，被标记为科陆电子的利好
- **根因**：`name_index` 用关键词索引个股（如"电子"→科陆电子），威帝股份公告标题含"威帝电子"，匹配到"电子"关键词后串到科陆电子
- **修复**：`get_hot_trend_data()` 匹配方式2中，增加 `has_specific_stock` 判断——有明确个股代码归属的公告（通过API字段或标题提取），只匹配到对应个股，不再进行名称关键词匹配
- **逻辑**：`has_specific_stock = len(news.get('stock_codes', [])) > 0 or bool(news.get('stock_code_api'))`
- GitHub推送：`fix: 有明确个股代码的公告不再通过关键词串到其他个股`

## 竞价选股优化（20:06）
- **需求**：竞价金额/成交量/换手率只算9:15-9:25竞价时段，新增"竞价涨幅"列
- **`fetch_auction_data` 改造**：
  - 返回新增 `auction_amount`（竞价金额，元）、`auction_change_pct`（竞价涨幅%）
  - 竞价涨幅 = (当前竞价价 - 昨收) / 昨收 × 100
- **`auction_formula_scan` 改造**：扫描前调用 `fetch_auction_data`，将竞价数据传给 `_run_formula_scan`
- **`_run_formula_scan` 改造**：
  - 新增 `auction_data: Dict = None` 参数
  - 有竞价数据时，用 `auction_amount/auction_vol` 覆盖 `s_dict['amount']/['volume']`
  - 竞价换手率 = `auction_vol(手) × 100 / 流通股数 × 100%`
- **前端 `FormulaPanel.tsx`**：
  - 接口新增 `auction_change_pct: number`
  - 表格新增「竞价涨幅」列（涨幅列之后），共9列
  - 支持按竞价涨幅排序
- **修复**：`fetch_auction_data` 中 try/except 缩进错误导致 SyntaxError
- GitHub推送：`feat: 竞价选股改用以竞价9:15-9:25专属数据，前端新增「竞价涨幅」列`
=== 2026-05-18 ===
修复国内政策分类：有个股代码归属的公告强制归为个股类型，不再归入政策；policy关键词移除'公告'避免误匹配
修复竞价选股：
- change_pct 显示实时行情涨幅（非竞价涨幅）
- turnover_pct/float_cap 补充 MARKET_CENTER_CACHE fallback
- 前端 API_BASE 改用 import.meta.env.VITE_API_BASE 走代理
个股公告全称精准匹配：
- 新增 extract_stock_name_from_title() 提取公告标题中的公司全称
- stock_by_name dict 支持全称精确匹配
- 匹配优先级：股票代码 > 个股全称 > 关键词

## 最强风口无数据修复（21:10）
- **现象**：最强风口页面全部显示0，风口股票/平均分数/采集新闻/利多股票均为0
- **根因**：`hot_trend.py` 第651行使用了 `has_specific_stock` 变量，但该变量在第689行（匹配方式3中）才定义，导致 `UnboundLocalError`，整个 `get_hot_trend_data()` 异常退出返回空数据
- **修复**：将 `has_specific_stock` 的定义移到 `for news in analyzed_news:` 循环开头（第617行之后），同时删除匹配方式3中重复的定义
- **验证**：修复后返回50只股票、7条新闻，数据正常
- **GitHub推送**：`fix: 修复最强风口has_specific_stock变量未定义导致无数据`

## 新闻汇总中心（21:44）
- **需求**：汇总国际新闻/国内政策/个股信息到统一页面
- **后端**：`main.py` 新增 `/api/news/all` 端点
  - 汇总 `_fetch_market_news()` 的市场新闻
  - 按关键词分类：policy(政策)/macro(国际)/industry(行业)/general(市场)
  - 返回各分类统计（条数、平均情绪）和新闻列表
- **前端**：`NewsHub.tsx` 新闻汇总组件
  - 分类卡片展示（政策/国际/行业/市场资讯）
  - Tab切换不同分类查看
  - 情绪标签（利多/利空/中性）
  - 每5分钟自动刷新
- **集成**：App.tsx 导航栏新增"新闻汇总"标签（绿色图标）
- **API测试**：正常返回30条新闻，分类统计正确
- GitHub推送：`feat: 新增新闻汇总中心，统一查看政策/国际/行业/个股信息`
