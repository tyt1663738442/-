# 2026-05-16 工作日志

## 行情看板问题修复（上午）

### 问题
1. 搜索框无效（只过滤已加载列表）
2. 自选股有两套独立数据（dashboard/watchlist vs big-orders/watchlist），不同步
3. 行情列表只显示 200 条，没有全量数据和分页
4. 股票名称列表区为空（因 watchlist 空导致整个列表空白）

### 修复内容

**后端 main.py**：
- 重写 `/api/stocks` 接口：支持 `page` 参数分页、全量 3001 只股票（FULL_STOCK_LIST）、搜索时从 CSV 元数据快速匹配
- `/api/dashboard/watchlist` 改为代理到 `/api/big-orders/watchlist`，两者完全同步

**前端 TradingLayout.tsx**：
- 自选股统一用 `/api/big-orders/watchlist`
- 行情列表用 `/api/stocks?limit=100&page=N`，分页加载
- 搜索：防抖 300ms + 调用 `/api/stocks?search=xxx`，支持代码/名称全量搜索
- 搜索有内容时高亮搜索框，清空 ×按钮

**前端 StockListPanel.tsx**：
- 新增分页 UI（翻页按钮 + 5个页码）
- 名称列明确设置 maxWidth + title tooltip
- 选中行左侧蓝色 border 标识
- 空状态提示区分搜索/自选/无数据

### 服务状态
- 后端：http://localhost:8000（PID 约 1687）
- 前端：http://localhost:8081
